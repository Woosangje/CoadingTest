using System;
using static System.Console;
using System.Collections;
using System.Collections.Generic;
using System.Text.RegularExpressions;
namespace ardent_21_02 {
    //문제 21-1 알파벳 반전



/*
class MainApp {

    static void Main(string[] args) {

        WriteLine("2명의 이름과 나이입력");
        string str1 = ReadLine();
        string str2 = ReadLine();
        string num = Regex.Replace(str1, @"\D", "");
        string strName1 = Regex.Replace(str1, @"[^a-zA-Zㄱ-ㅎ가-힣]", "");
        string strName2 = Regex.Replace(str2, @"[^a-zA-Zㄱ-ㅎ가-힣]", "");
        int strAge1 = int.Parse(Regex.Replace(str1, @"[^0-9]", ""));
        int strAge2 = int.Parse(Regex.Replace(str2, @"[^0-9]", ""));

        if(strName1 == strName2) {

            WriteLine("이름이 동일합니다!");
        }
        else {
            WriteLine("이름이 동일하지 않습니다!");
        }

        if(strAge1 == strAge2) {
            WriteLine("나이가 같습니다!");
        }
        else {
            WriteLine("나이가 같지 않습니다1");
        }
    }

}*/


