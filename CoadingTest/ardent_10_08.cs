using System;
using static System.Console;

namespace ardent_10_08
{
    //도전 8
    //2를 n횟수만큼곱하는것

    /*
   class MainApp
   {
       static void Main(string[] args)
       {
           Write("정수 입력 :");
           int n = int.Parse(ReadLine());
           int count = n;
           int result = 1;
           Recursion(count,ref result);
           WriteLine("2의 {0}승은 {1}", n, result);
       }
       static void Recursion(int count,ref int result)
       {
           if(count > 0)
           {
               result *= 2;
               Recursion(count - 1, ref result);
           }
       }

   }*/
}


